import { Noto_Serif_Khojki } from 'next/font/google';

export const titleFont = Noto_Serif_Khojki({ subsets: ['latin'], fallback: ['system-ui'] });
export const mainFont = Noto_Serif_Khojki({ subsets: ['latin'], fallback: ['system-ui'] });
